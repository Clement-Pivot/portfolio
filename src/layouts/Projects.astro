---
import projects from '../../public/projects.json'
import { ProjectCard } from '../components/ProjectCard/index.tsx'
---

<div id="my-projects" class="min-h-screen snap-start">
  <h2 class="categorie">Mes projets</h2>
  <div
    class="mt-1 grid grid-cols-[repeat(auto-fill,minmax(15rem,1fr))] justify-items-center gap-y-4"
  >
    {projects.map((elem) => <ProjectCard {...elem} client:load />)}
  </div>
</div>
